{
    "name":        "validate",
    "description": "@todo",
    "version":     "0.0.1",

    "author": {
        "name":  "Dmitry Kirilyuk",
        "email": "gk.joker@gmail.com"
    },

    "repository": {
        "type": "git",
        "url":  "https://github.com/Jokero/validate.git"
    },

    "license": "MIT",

    "keywords": [
        "@todo"
    ],

    "engines": {
        "node": ">=4.0.0"
    },

    "devDependencies": {
        "browserify":          "13.0.1",
        "babel-preset-es2015": "6.6.0",
        "babelify":            "7.3.0",
        "uglify-js":           "2.6.2",
        "chai":                "3.5.0",
        "mocha":               "2.4.5"
    },

    "scripts": {
        "test":       "mocha 'tests/**/*.js' --timeout 5000",
        "browserify": "browserify index.js --transform [ babelify --presets es2015 ] --standalone validate --outfile dist/validate.js",
        "minify":     "uglifyjs dist/validate.js --output dist/validate.min.js",
        "build":      "npm run browserify && npm run minify"
    }
}